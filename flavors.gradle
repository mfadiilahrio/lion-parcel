ext.flavorConfig = {

    flavorDimensions "default"
    productFlavors {
        staging {
            versionNameSuffix "-staging"
        }
        production {
            isDefault.set(true)
        }
    }
}